<section class="facilitator" id="facilitacao">
  <div class="section-head">
    <p class="kicker">05 / FACILITAÇÃO</p>
    <div>
      <h2>Estrutura própria. Facilitação em rede.</h2>
      <p>
        O BNW3H é uma entidade própria fundada por Marcelo. A ReReGeneradora
        facilita leitura de contexto, desenho de governança e acesso a conexões,
        sem assumir a propriedade, a economia ou a autoridade do Hub.
      </p>
    </div>
  </div>

  <div class="relationship">
    <article>
      <span>BNW3H</span>
      <strong>O organismo</strong>
      <p>Marca, território, operação, IP, governança e direitos econômicos próprios.</p>
    </article>
    <div aria-hidden="true">
      <i></i>
      <b>FACILITA</b>
      <i></i>
    </div>
    <article>
      <span>REREGENERADORA</span>
      <strong>A camada de integridade</strong>
      <p>Contexto, proximidade, estruturação, governança e ponte de capital.</p>
    </article>
  </div>

  <p class="boundary">Compartilhar um campo não significa compartilhar automaticamente operação, capital, IP, estrutura legal ou métricas.</p>
</section>

<style>
  .facilitator {
    padding: clamp(5rem, 10vw, 9rem) 1rem;
    background: #f1ecdf;
    color: #102019;
  }

  .section-head,
  .relationship,
  .boundary {
    width: min(100%, 80rem);
    margin-inline: auto;
  }

  .section-head {
    display: grid;
    grid-template-columns: minmax(8rem, 0.55fr) minmax(0, 2fr);
    gap: 2rem;
  }

  .kicker,
  article > span {
    color: #5d6d65;
    font-size: 0.65rem;
    font-weight: 900;
    letter-spacing: 0.14em;
  }

  h2 {
    max-width: 15ch;
    font-family: Georgia, "Times New Roman", serif;
    font-size: clamp(2.8rem, 6vw, 6rem);
    font-weight: 400;
    line-height: 0.95;
    letter-spacing: -0.055em;
  }

  .section-head div > p {
    max-width: 43rem;
    margin-top: 2rem;
    color: #52635a;
    font-size: 1rem;
    line-height: 1.75;
  }

  .relationship {
    display: grid;
    grid-template-columns: 1fr auto 1fr;
    align-items: center;
    gap: 1.5rem;
    margin-top: clamp(4rem, 8vw, 7rem);
  }

  article {
    min-height: 22rem;
    padding: 2rem;
    display: flex;
    flex-direction: column;
    justify-content: flex-end;
    border: 1px solid #10201930;
    border-radius: 45% 55% 52% 48% / 54% 44% 56% 46%;
  }

  article:last-child {
    border-radius: 54% 46% 43% 57% / 43% 55% 45% 57%;
  }

  article strong {
    margin-top: 0.8rem;
    font-family: Georgia, "Times New Roman", serif;
    font-size: clamp(2rem, 4vw, 3.6rem);
    font-weight: 400;
    line-height: 1;
    letter-spacing: -0.045em;
  }

  article p {
    max-width: 30rem;
    margin-top: 1rem;
    color: #617168;
    font-size: 0.82rem;
    line-height: 1.65;
  }

  .relationship > div {
    display: grid;
    place-items: center;
    gap: 0.75rem;
    color: #738078;
  }

  .relationship > div i {
    width: 1px;
    height: 4rem;
    background: #10201940;
  }

  .relationship > div b {
    font-size: 0.58rem;
    letter-spacing: 0.16em;
    writing-mode: vertical-rl;
  }

  .boundary {
    margin-top: 3rem;
    padding-top: 2rem;
    border-top: 1px solid #10201930;
    color: #617168;
    font-size: 0.72rem;
    text-align: center;
  }

  @media (max-width: 44rem) {
    .section-head,
    .relationship {
      grid-template-columns: 1fr;
    }

    .relationship > div {
      grid-template-columns: 1fr auto 1fr;
    }

    .relationship > div i {
      width: 4rem;
      height: 1px;
    }

    .relationship > div b {
      writing-mode: horizontal-tb;
    }

    article {
      min-height: 20rem;
    }
  }
</style>
